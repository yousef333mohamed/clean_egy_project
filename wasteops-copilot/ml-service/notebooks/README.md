# Notebooks

Exploration notebooks may inspect sampled, non-sensitive data. Production feature, training, evaluation, and promotion logic must live in tested Python modules; notebooks are never deployment artifacts.
